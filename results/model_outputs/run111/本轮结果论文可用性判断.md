# 本轮结果论文可用性判断

本次运行编号：run_111。

本次最优模型：Bi-LSTM。

截至目前综合表现最好的是：run_111。

是否建议基于该编号生成论文图表：是。

判断依据：

- R² 最优模型：Bi-LSTM
- RMSE 最优模型：Bi-LSTM
- MAE 最优模型：Bi-LSTM
- RAE 最优模型：Bi-LSTM
- Bi-LSTM 是否在核心指标上整体优于 RF 和 LSTM：是
- Bi-LSTM 是否满足论文要求：是
- Bi-LSTM 是否达到推荐目标：是

阈值口径：

- 最低使用阈值：R² ≥ 0.50、RAE < 0.75、nRMSE ≤ 15%、nMAE ≤ 10%。
- 推荐目标：R² ≥ 0.60、RAE < 0.70、nRMSE ≤ 12%、nMAE ≤ 8%。
- nRMSE/nMAE 低不等于模型达标；核心仍看 R²、RAE 以及 Bi-LSTM 是否整体优于 RF 和 LSTM。

说明：Bi-LSTM在R²、RMSE、MAE、RAE等核心指标上整体优于RF和LSTM，且达到论文最低使用阈值；nRMSE/nMAE仅作为辅助约束，不能替代R²和RAE判断。
